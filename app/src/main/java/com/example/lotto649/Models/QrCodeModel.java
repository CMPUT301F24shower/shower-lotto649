package com.example.lotto649.Models;

import com.example.lotto649.AbstractClasses.AbstractModel;

/**
 * Model class for generating QR codes associated with events.
 * Extends AbstractModel to inherit common model functionality.
 */
public class QrCodeModel extends AbstractModel {

    /**
     * Generates a QR code for the specified event.
     *
     * @param event The EventModel instance for which to generate the QR code.
     * @return An Object representing the generated QR code (to be implemented).
     */
    public static Object generateForEvent(EventModel event){
        //Add actual code for QrCode gen
        return "";
    }
}
